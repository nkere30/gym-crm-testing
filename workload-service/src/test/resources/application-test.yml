spring:
  activemq:
    broker-url: vm://embedded?broker.persistent=false
  data:
    mongodb:
      database: testdb
  cloud:
    discovery:
      enabled: false

eureka:
  client:
    enabled: false
app:
  messaging:
    training:
      queue: training.events
      dlq: training.events.dlq