spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/workload_stg
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
  activemq:
    broker-url: tcp://localhost:61616
  jms:
    pub-sub-domain: false
    listener:
      min-concurrency: 3
      max-concurrency: 12

app:
  messaging:
    training:
      queue: training.events
      dlq: training.events.dlq
      max-redeliveries: 3
